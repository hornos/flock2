127.0.0.1	localhost
127.0.1.1	debian

# The following lines are desirable for IPv6 capable hosts
::1     localhost ip6-localhost ip6-loopback
ff02::1 ip6-allnodes
ff02::2 ip6-allrouters

{% for host in hostvars %}
{{hostvars[host][ansible_paths[interfaces.system]]['ipv4']['address']}} {{host}} {{host}}.{{domains.system}}
{% endfor %}
